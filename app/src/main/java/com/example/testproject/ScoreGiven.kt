package com.example.testproject

class GivingScore(var score: String)
{

    fun current(item: String, item_rating: Int)
    {
        var rating_score: Int = 500
        score += rating_score
    }
}