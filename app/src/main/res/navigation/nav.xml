<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav"
    app:startDestination="@id/giverScore">
    <fragment
        android:id="@+id/giverScore"
        android:name="com.example.testproject.GiverScore"
        android:label="giver_score_fragment"
        tools:layout="@layout/giver_score_fragment" >
        <action
            android:id="@+id/action_giverScore_to_profTemp"
            app:destination="@id/profTemp" />
    </fragment>
    <fragment
        android:id="@+id/profTemp"
        android:name="com.example.testproject.ProfTemp"
        android:label="prof_temp_fragment"
        tools:layout="@layout/prof_temp_fragment" >
        <action
            android:id="@+id/action_profTemp_to_giverScore"
            app:destination="@id/giverScore" />
    </fragment>
</navigation>